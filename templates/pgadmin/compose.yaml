services:
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    restart: unless-stopped
    ports:
      - '80:80'
    env_file:
      - .env
    volumes:
      - ./pgadmin:/var/lib/pgadmin/
